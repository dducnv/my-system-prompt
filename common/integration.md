**Context:** The UI for [Component Name] is ready. Now, I need to connect it to the application state.
**Task:** Integrate this UI with our [State Management, e.g., BLoC / Riverpod] provider.

**Requirements:**

1. **State Injection:** Map the UI buttons to the corresponding Events/Methods in the provider.
2. **Reactive UI:** Ensure the widget listens to the state changes (e.g., update the counter when an item is added).
3. **Error Handling:** If the state returns an error, display [e.g., a red border or a shake animation].

**Output:** Provide the integration code. Use clean dependency injection patterns.
