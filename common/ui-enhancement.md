**Context:** Based on the [Feature Name, e.g., Add to Cart] logic we just implemented.
**Task:** Enhance the user experience by adding a professional animation to this interaction.

**Requirements:**

1. **Animation Style:** [e.g., A subtle scale-up bounce effect / A slide-in snackbar / A particle burst].
2. **Trigger:** [e.g., When the user taps the button].
3. **Constraints:** - Keep the duration between 200ms - 400ms for a snappy feel.
   - Use a `Curves.easeOut` or `Curves.elasticOut` for a premium "fancy" touch.
   - Ensure it does not cause unnecessary rebuilds of the entire screen.

**Output:** Update the existing code to include this animation. Only provide the modified parts or the updated widget.
