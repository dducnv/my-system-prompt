**Context:** We have the functional [Feature Name] working. Now, let's make the logic more robust.
**Task:** Add the following validation and feedback mechanisms.

**Requirements:**

1. **Validation:** [e.g., Prevent adding more than 10 items to the cart].
2. **Feedback:** [e.g., Show a toast message if the item is out of stock].
3. **Safety:** [e.g., Implement a "debounce" or "loading state" to prevent multiple rapid taps on the button].

**Output:** Refactor the logic part of the code to handle these cases. Keep the UI code untouched unless necessary.
